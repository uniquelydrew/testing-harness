name: gtk4-demo-values
version: 1
backend: {kind: gtk-demo, example: spinbutton}
requires: [local-only, gui, components, atspi, value-control]
components: resources/components.yaml
tests: [tests/test_values.py]
