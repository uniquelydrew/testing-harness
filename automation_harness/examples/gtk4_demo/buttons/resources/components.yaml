version: 1
components:
  gtk_demo.buttons.main:
    description: GTK 4.14 Dialog demo message-dialog launcher.
    actions: [resolve, activate]
    strategies:
      - type: atspi
        identification:
          mandatory: {name: Message Dialog, role: push button}
          assistive: {application: gtk4-demo, window: Dialogs}
