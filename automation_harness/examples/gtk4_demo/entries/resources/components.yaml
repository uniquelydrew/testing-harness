version: 1
components:
  gtk_demo.entries.text:
    description: GTK 4.14 Undo and Redo editable text entry.
    actions: [resolve]
    strategies:
      - type: atspi
        identification:
          mandatory: {name: Use Control+z or Control+Shift+z to undo or redo changes, role: text}
          assistive: {application: gtk4-demo, window: Undo and Redo}
