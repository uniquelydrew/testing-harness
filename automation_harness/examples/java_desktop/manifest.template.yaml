name: java-desktop-visual-regression
version: 1
requires: [local-only, gui, components, java-accessibility, screen-capture, vision]
backend:
  kind: live-desktop
  # Launch the application before this legacy Python bundle, or migrate the
  # workflow to a declarative plan with a contract-backed setup step.
tests: [tests/test_visual_workflow.py]
components: components.yaml
